vagrant init ubuntu/trusty64
vagrant up 
vagrant ssh

sudo apt-get update
sudo apt-get upgrade

sudo apt-get autoremove
sudo apt-get install finger

sudo adduser student
password: student
finger student

ssh student@127.0.0.1 -p 22
sudo nano /etc/ssh/sshd_config
Port 22 --> 2222
sudo service ssh force-reload
ssh student@127.0.0.1 -p 2222

student: sudo cat /etc/passwd
vagrant: sudo cat /etc/sudoers
vagrant: sudo ls /etc/sudoers.d
vagrant: sudo cp /etc/sudoers.d/vagrant /etc/sudoers.d/student
vagrant: sudo nano /etc/sudoers.d/student
change "vagrant" on line 2 to "student"
save file

student: sudo cat /etc/passwd

generate public-private key pair on LOCAL MACHINE: 
ssh-keygen
/c/Users/thep3/.ssh/linuxCourse
passphrase: ilovebowie
SHA256:yPUHK+ZZaL/KaI9NtswrjWuGz6BThea9HzfzsjRk3OA thep3@DESKTOP-M033BO1

installing public key on remote server: 
mkdir .ssh
touch .ssh/authorized_keys

on local machine: 
cat .ssh/linuxCourse.pub

on remote server - student:
nano .ssh/authorized_keys
chmod 700 .ssh
chmod 644 .ssh/authorized_keys

on local machine: 
ssh student@127.0.0.1 -p 2222 -i ~/.ssh/linuxCourse.pub

STEP 26: Disable Password based logins
Conclusion: Server is configured on the Internet.


FINAL PROJECT: 
1. Get Lightsail Server
2. Secure your server. 

	Update Firewall for SSH 2200 and NTP 123

	sudo apt-get update
	sudo apt-get upgrade

	sudo nano /etc/ssh/sshd_config
	Add a line for port 2200
	Save this.

	Configure UFW:
	sudo ufw status
	sudo ufw default deny incoming
	sudo ufw default allow outgoing
 	sudo ufw allow ssh
	sudo ufw allow 2200/tcp
	sudo ufw allow www
	sudo ufw enable
	sudo ufw status
	reboot AWS Lightsail Server on website

	Login with Local Terminal:
	ssh -i .ssh/<your_private_key> <your_username>@<your_public_IP> -p 2200

	Remove Port 22: 
	- sudo nano
	- Firewall settings

	sudo service ssh force-reload
	ssh ubuntu@18.234.108.85 -p 2200

	Connect via Local Machine:
	ssh ubuntu@18.234.108.85 -p 2200 -i ~/.ssh/LightsailDefaultKey-us-east-1.pem

3. Give grader access.
4. Prepare to deploy your project. 
5. Deploy the Item Catalog project.


CONNECT TO LIGHTSAIL: 
1. Open Git Bash
2. ssh ubuntu@18.234.108.85 -p 2222 -i ~/.ssh/LightsailDefaultKey-us-east-1.pem

